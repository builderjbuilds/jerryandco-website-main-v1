{
  "name": "jerryandco-website2",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node scripts/check-cloud.mjs",
    "preview": "astro build && wrangler dev ./dist/server/entry.mjs --config ./dist/server/wrangler.json",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && wrangler deploy --config ./dist/server/wrangler.json",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.0.0",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.1",
    "resend": "^6.14.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "cloudinary": "^2.10.0",
    "dotenv": "^17.4.2",
    "sharp": "^0.35.2",
    "wrangler": "^4.103.0"
  }
}
